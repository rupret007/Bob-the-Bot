{
  "name": "bob-the-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Pinned Bob application contract over the Andrea messaging engine and guarded OpenClaw delegation.",
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "node --test",
    "verify": "node scripts/verify-contract.mjs",
    "verify:engine": "node scripts/verify-contract.mjs --engine .engine/Andrea_NanoBot --apply",
    "privacy:check": "node scripts/privacy-check.mjs",
    "check": "npm run test && npm run verify && npm run privacy:check"
  }
}
