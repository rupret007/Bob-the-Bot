# Non-secret Bob product defaults only.
# Copy individual values into an owner-controlled runtime configuration.
# Credential, endpoint, phone, account, and recipient fields are intentionally absent.
ASSISTANT_NAME=Bob
TELEGRAM_NATURAL_UX=true
BLUEBUBBLES_ENABLED=false
BLUEBUBBLES_SEND_ENABLED=false
BLUEBUBBLES_CONTROL_API_ENABLED=false
ANDREA_OPENAI_BACKEND_ENABLED=false
